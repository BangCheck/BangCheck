# codex critical-review artifact

- Provider: codex
- Model: gpt-5.6-terra
- Reasoning effort: high
- Target dir: /Users/jonghoPro/woo/00_project/08_BangCheck
- Exit code: 0
- Resolved CLI: /Users/jonghoPro/.local/bin/codex
- Started at (UTC): 2026-08-05T07:03:59.918451+00:00
- Finished at (UTC): 2026-08-05T07:05:59.529752+00:00

## Original task / label

guard-fix-scope

## Final prompt

```text
[실행 조건 — 먼저 읽어라]
codex exec 비대화형 실행이다. 되묻는 순간 답할 사람이 없어 통째로 실패한다.
2026-08-05 첫 시도가 정확히 그렇게 죽었다 — 승인 질문 한 줄만 반환하고 종료했다.

- 비용 승인은 끝났다. 진행 여부를 묻지 마라.
- 이 저장소 AGENTS.md STEP 1이 가리키는 _wood/workflows/_PROTOCOL.md 의 승인 게이트
  ("Do you want to proceed with this action? (Y/N)")는 이 요청에 적용되지 않는다.
  그것은 팀 이슈/PR 워크플로용이고 지금은 읽기 전용 검토다.
- (Y/N) 질문·확인 요청·메뉴 출력 금지. 첫 출력부터 본문으로 시작해라.

[요구하는 것]
이번엔 비판만이 아니라 **결정**을 내려라. 사용자가 두 질문의 답을 너에게 위임했다.
반박은 근거로 쓰고, 마지막에 실행 가능한 확정안 하나를 내라.

## 대상 — 직접 읽어라 (read-only)
- .github/workflows/compliance-guard.yml   (7규칙 전체)
- .github/workflows/protected-files.yml    (PROTECTED_PATTERNS)
- .github/workflows/atlas-resolve.yml      (오늘 추가, 이미 green)
- .github/PULL_REQUEST_TEMPLATE.md
- .github/CODEOWNERS
- AGENTS.md                                (Protected Files 목록 / Role Verification)
- _wood/workflows/_compliance-spec.yaml    (스스로 "Used by: GitHub Actions"라 선언)
- _wood/workflows/_protocol.md
- .claude/commands/swyp-pr.md, swyp-issue.md
- .claude/hooks/protected-gate.sh

## 이미 끝난 교차검증 (반복 금지)
critical-review-artifacts/codex-issue-pr-harness-design-2026-08-04T23-20-35-697398Z.md
critical-review-artifacts/codex-pr-pattern-first-judgment-retry-2026-08-05T04-12-38-625770Z.md
수용된 결론: repo identity, CODEOWNERS에 코드 경로 없음, 정합 후 점진 강제,
changed-files↔implementedBy 대조 불가, PR은 provenance/registry는 현재 귀속,
확정 순서(runtime→baseline + RTE-02 → 브랜치보호 → Issue/PR 선언형식 → PR 선언 대조).

## 오늘 실측 (검증 대상 — 틀렸으면 지적해라)
- 머지 PR 5건의 본문 섹션이 100% `Summary / Linked Issue / Changes / Checklist`.
  레포 PULL_REQUEST_TEMPLATE.md 는 `PR Description / Screenshots / Notes / Checklist`.
  guard Rule 4 는 `What / Why / Test` 를 찾는다. 셋이 전부 다르다.
- 저장소 실제 라벨: 유형:버그, 유형:작업, 높음/보통/낮음(접두 없음), 백엔드/프론트엔드,
  compliance-violation, compliance-warning + GitHub 기본 9종.
  `순위:` 접두 라벨과 `상태:` 라벨은 존재하지 않는다. Rule 5는 통과가 구조적으로 불가.
  최근 이슈 15건 중 13건이 라벨 0개.
- 머지 20건 브랜치 타입: feat 11, fix 7, revert 1, 규약 이탈 1(wave3-fix-home).
  Rule 1 허용 목록에 revert 없음.
- skip-compliance 라벨은 저장소에 존재하지 않고 사용 이력 0건.
- Rule 7은 files 조회를 per_page 300 한 페이지만 한다.
- guard는 yq를 설치만 하고 _compliance-spec.yaml 을 읽지 않는다. 규칙은 워크플로에 하드코딩.
- protected-files.yml 의 PROTECTED_PATTERNS 는 AGENTS.md 목록 대비
  GEMINI.md, .claude/hooks/**, .claude/settings.json, _wood/context/**, docs/spec/**
  가 빠져 있고 compliance-guard.yml 자신도 없다. CODEOWNERS에는 compliance-guard.yml이 있다.
- 오늘 연 PR #216에서 guard가 warning 2건을 실제로 냈다: pr_template(What/Why 누락),
  issue_labels(순위:/상태: 누락). 둘 다 위 오탐이다. errors는 0.
- main 브랜치 보호 없음(404), rulesets [].
- .claude/hooks/protected-gate.sh 는 tool_input.file_path 를 최상위 file_path 로 읽어
  항상 통과시키고 있었다(오늘 수정본이 있으나 아직 커밋 안 됨).

## 결정해야 할 것 1 — 이번에 고칠 범위
후보 결함:
  A Rule 4 섹션 불일치 (오탐)
  B Rule 5 존재하지 않는 라벨 요구 (오탐)
  C Rule 1 revert 타입 누락 (오탐)
  D skip-compliance 우회에 권한 검사 없음 (보안)
  E Rule 7 per_page 300 절단 (보안)
  F guard가 _compliance-spec.yaml 을 읽지 않음 (죽은 배선)
  G protected-files.yml 목록 5개 누락 (보안)

Claude의 제안은 "A·B·C·D·E·G 를 고치고 F는 별도 이슈"였다.
반박 지점: 이 묶음이 옳은가? F를 빼면 "선언은 한 곳" 원칙을 어기는 상태가 계속된다.
반대로 F를 넣으면 7규칙 재작성이라 회귀 위험이 크다. 어느 쪽인가?
B를 "실재하는 라벨만 검사"로 완화하면, 나중에 ISSUE_TEMPLATE으로 라벨을 강제할 때
규칙을 또 고쳐야 한다 — 두 번 고치는 것이 맞는가, 아니면 지금 Rule 5를 끄고
ISSUE_TEMPLATE 도입 시점에 한 번에 켜는 것이 맞는가?
A를 고칠 때 정본을 무엇으로 잡아야 하는가 — 실측 관행(Summary/Linked Issue/Changes)인가,
레포 PULL_REQUEST_TEMPLATE.md 인가? 둘 중 하나를 다른 쪽에 맞춰야 하는데 어느 방향인가?

## 결정해야 할 것 2 — skip-compliance 우회
후보: (i) 제거 (ii) 라벨 부여자 Admin 확인 추가 (iii) 그대로 둠
Claude는 (i)을 권했다. 근거는 라벨도 없고 사용 이력도 0건이라는 것.
반박 지점: 우회 수단이 아예 없는 게 옳은가? 긴급 배포·hotfix 상황에서
guard가 오탐으로 막으면 어떻게 되는가? required check로 승격한 뒤라면?
(ii)를 고른다면 GitHub Actions에서 "라벨을 단 사람"을 신뢰성 있게 확인할 수 있는가 —
timeline API로 가능한가, 경합(라벨 추가 후 다른 사람이 재실행)은 어떻게 되는가?
실행 가능한 방식이 아니면 (ii)를 권하지 마라.

## 출력
1) 위 실측 중 틀린 것 (있으면)
2) 결함별 판정 — 고칠 것/미룰 것/폐기할 것, 각각 한 줄 근거
3) 결정 1의 확정안 (무엇을 이번 PR에 넣는가)
4) 결정 2의 확정안 (i/ii/iii 중 하나 + 이유)
5) 놓친 결함이 있으면 추가
간결하게. 동의는 한 줄로.

```

## Final response

```text
동의: A·B·C·D·E·G만 고치고 F를 미루자는 안에는 동의하지 않는다.

1) 틀린/불완전한 실측

- `compliance-guard.yml` 누락은 AGENTS 기준 5개가 맞다. 다만 `compliance-guard.yml` 자체는 AGENTS 목록에는 없고 CODEOWNERS에만 있으므로, 이는 별도의 여섯 번째 보호정책 불일치다.
- hook 결함은 맞다. 현 코드는 최상위 `file_path`만 읽어 중첩 `tool_input.file_path`를 놓친다. 다만 이 작업트리에는 “오늘 수정한 미커밋본”이 없다(`git status`/diff 깨끗함).
- 나머지 GitHub 실측은 네트워크 연결 실패로 재조회하지 못했지만, 제시된 오탐 원인은 저장소 코드와 일치한다.

2) 결함별 판정

- A 고칠 것 — 정본은 `Summary / Linked Issue / Changes / Checklist`; 최근 관행 100% 및 `swyp-pr`와 맞으므로 GitHub PR 템플릿을 여기에 맞춘다.
- B 이번 강제는 폐기 — “실재 라벨만 검사”로 완화하지 말고 Rule 5를 비활성화한다. ISSUE_TEMPLATE·라벨 체계가 확정될 때 정확한 요구사항으로 한 번만 재도입한다.
- C 고칠 것 — `revert/{issue}-{slug}`를 허용하고, Rule 3도 표준 Git revert 커밋 메시지를 예외 처리한다.
- D 고칠 것 — `skip-compliance` 우회를 제거한다.
- E 고칠 것 — `listFiles`를 완전 페이지네이션한다.
- F 고칠 것 — 선언이 “GitHub Actions가 사용”이라고 말하는 이상, 실행기가 spec을 읽지 않는 상태를 남기면 이번 수정도 다시 드리프트한다.
- G 고칠 것 — AGENTS 기준 5개와 `compliance-guard.yml`을 보호 목록에 포함한다.

3) 결정 1 확정안

이번 PR에는 A·C·D·E·F·G와 Rule 5 비활성화를 넣는다.

- `_compliance-spec.yaml`을 실제 런타임 정본으로 연결하고, `enabled`·severity·패턴·섹션·파일 규칙을 거기서 읽는다.
- `commit_issue_ref`처럼 spec에는 enabled인데 런타임에 없는 규칙은 몰래 활성화하지 말고, 이번 PR에서 명시적으로 비활성화하거나 구현한다. 권고는 관측 기준선 전까지 비활성화다.
- PR 템플릿과 spec을 `Summary / Linked Issue / Changes / Checklist`로 통일한다.
- 301번째 파일, 31번째 커밋, revert, Rule 5 비활성화, 실제 PR 본문 형식의 회귀 테스트를 추가한다.
- 보호 목록은 5개 누락분과 `compliance-guard.yml`을 포함해 AGENTS·CODEOWNERS·workflow의 범위를 일치시킨다.
- 이 PR이 안정적으로 green인 기준선을 만든 뒤에만 main 보호/ruleset에서 해당 check를 required로 승격한다.

4) 결정 2 확정안

(i) 제거.

(ii)는 timeline API로 라벨을 단 actor 자체는 확인할 수 있지만, 그 승인은 특정 head SHA에 묶이지 않아 이후 커밋·재실행에도 남는다. 따라서 “Admin이 한 번 붙인 라벨”은 변경된 PR을 계속 우회시킬 수 있어 안전한 권한 검사가 아니다. GitHub의 timeline event에는 actor가 제공되지만 이 한계는 해소하지 못한다. [GitHub timeline events API](https://docs.github.com/en/rest/issues/timeline)

긴급 상황의 탈출구는 PR 라벨이 아니라, required check 승격 시 GitHub의 감사 가능한 Admin bypass 정책으로 둔다. 그 전까지는 오탐을 P0로 수정한다.

5) 추가 결함

- Rule 3의 `listCommits`도 페이지네이션이 없어 기본 첫 페이지 커밋만 검사한다.
- protected-files 승인도 이전 head의 Admin 승인으로 새 push를 통과시킬 수 있다. 승인 review의 `commit_id`를 현재 head와 대조하거나, branch protection의 stale-approval dismissal을 사용해야 한다.
- spec의 PR-size 예외·400-line caution도 런타임에 반영되지 않는다.
- 현재 origin은 `BangCheck/BangCheck`인데 프로토콜의 preflight와 여러 워크플로는 `SWYP-Backend/project`를 하드코딩한다. 읽기 전용 검토와 별개로, 실행 워크플로는 현재 그대로면 잘못된 저장소에서 멈춘다.
```

## Concise summary

codex 실행 완료. Final response에서 verdict와 finding을 확인할 것.
