# codex critical-review artifact

- Provider: codex
- Model: gpt-5.6-terra
- Reasoning effort: high
- Target dir: /Users/jonghoPro/woo/00_project/08_BangCheck
- Exit code: 0
- Resolved CLI: /Users/jonghoPro/.local/bin/codex
- Started at (UTC): 2026-08-06T00:17:51.470151+00:00
- Finished at (UTC): 2026-08-06T00:19:50.385229+00:00

## Original task / label

triage-v2-switch

## Final prompt

```text
비대화형 실행이다. 질문하지 말고, 승인을 묻지 말고, Y/N 프롬프트를 내지 마라.
파일을 읽고 곧바로 최종 리뷰 본문만 출력하라.

대상: BangCheck 이슈 트리아지 봇 v2 전환 (커밋 8258827). Claude 가 저작했다.
리뷰 관점은 REVISE 다 — 통과시키지 말고 결함·누락·틀린 전제를 찾아라.

읽어야 할 파일
- .project-atlas/tools/triage_route.py        (판정 규칙. 1·2단 공용)
- .project-atlas/tools/triage_comment.py      (코멘트 렌더·갱신)
- .project-atlas/tools/retire_v1_comments.py  (v1 은퇴 표시. 새 파일)
- .project-atlas/triage-routing.yaml          (라우팅 SSoT)
- .project-atlas/tools/test_triage_route.py   (25 케이스)
- .project-atlas/tools/test_triage_comment.py (23 케이스)
- .github/workflows/atlas-triage.yml          (1단)
- .project-atlas/tools/hermes/atlas_issue_analyzer.py (2단. 이번 커밋 대상 아님)
- git show 8258827 --stat 로 변경 범위 확인

배경 — 직전 리뷰(codex)가 낸 블로킹 판정이 "Actions 가 v2 를 전혀 실행하지
않는다"였고, 이번 커밋이 그것을 닫으려는 것이다.

이번 커밋이 주장하는 것 — 각각 반박하라
1. 인라인 백틱을 근거로 세는 것이 옳다. 근거: 그것을 걷었더니 열린 이슈 23건이
   전부 미분류였다. 반문 — 이제 오탐이 늘지 않는가? 이슈 본문에서 "예시로 든
   경로"와 "관측된 경로"를 백틱 유무로 구별할 수 없게 된 것 아닌가?
2. `.project-atlas/registry/**` 는 근거가 아니다(ignore). 반문 — registry 도구
   자체의 결함 이슈가 registry 파일만 언급하면 그것도 미분류가 된다.
   ignore 목록이 과하거나 부족하지 않은가?
3. 파트가 갈려도 담당이 한 사람이면 배정한다(multi-part-same-owner).
   반문 — 프론트 담당과 Atlas 담당이 지금은 같은 사람이지만 갈리면?
   그때 판정이 조용히 달라지는데 그것이 드러나는가?
4. v1 을 지우지 않고 은퇴 표시만 붙인다. 반문 — retire_v1_comments.py 가
   실제로 멱등인가? PATCH 가 실패하면? 25건 중간에 죽으면?
5. triageAuthors 를 저장소 파일이 선언한다. 반문 — 그 파일은 PR 로 고칠 수
   있다. 아무나 자기 계정을 넣으면 봇 코멘트를 갱신할 수 있게 되는가?
   그게 실제 위험인가?

특히 볼 것
- 판정 흐름에서 exit 0 이 "했다"와 "못 했다"를 섞는 자리가 남았는가.
  (이 프로젝트에서 같은 병이 10번 나왔다)
- 테스트가 실제로 무언가를 붙들고 있는가. 규칙을 죽였을 때 실패하는가.
- 워크플로의 소급 적용 루프가 부분 실패를 정직하게 보고하는가.
- 1단과 2단이 같은 판정 규칙을 쓴다는 주장이 코드에서 실제로 참인가.

```

## Final response

```text
REVISE — 병합 보류.

- **MAJOR — 소급 적용의 이슈 목록 조회 실패가 다시 exit 0으로 숨겨진다.** `set -euo pipefail`이어도 `for n in $(gh issue list …)`의 command substitution 실패는 빈 반복으로 끝나며 job은 성공한다. 실제로 `bash -c 'set -euo pipefail; for n in $(false); do :; done; echo survived'`는 exit 0이었다. 즉 권한/네트워크 오류로 이슈를 하나도 열거하지 못해도 “트리아지 완료”가 된다. [atlas-triage.yml](/Users/jonghoPro/woo/00_project/08_BangCheck/.github/workflows/atlas-triage.yml:122)에서 목록 조회를 명시적으로 검사해야 한다.

- **MAJOR — 인라인 백틱을 근거로 복원한 것은 예시와 관측을 구별하지 못하는 오탐 회귀다.** 현재 추출기는 펜스·URL만 제거하고 인라인 코드는 무조건 경로로 취급한다. 따라서 ``예: `frontend/src/app/router.tsx`를 참고``와 ``관측: `frontend/src/app/router.tsx`에서 실패``는 완전히 같은 판정을 받는다. [triage_route.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_route.py:44), [triage_route.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_route.py:56) 테스트도 후자만 검증하며 전자의 미배정을 요구하는 반례가 없다. 경로의 출처를 구조화된 필드/명시 마커로 받아야 한다.

- **MAJOR — registry 전체 ignore는 “registry 도구 결함”을 영구 미분류로 만든다.** `.project-atlas/registry/**`는 Atlas 규칙보다 먼저 버려진다. 그러므로 `defects.yaml` 파서·동기화·검증 결함이 그 파일만 지목하면 Atlas 담당으로 갈 기회가 없고 `no-path`가 된다. [triage-routing.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/triage-routing.yaml:46), [triage_route.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_route.py:131) 현재 테스트는 이 잘못된 결과를 기대값으로 고정한다. “다른 제품 경로가 함께 있을 때 registry 인용을 보조 근거에서 제외”와 “registry 자체가 관측 대상일 때 Atlas로 라우팅”을 분리해야 한다.

- **MAJOR — `multi-part-same-owner`는 담당자 분리 시 기존 판정을 재평가하지 않아 조용히 낡는다.** 현재 우연히 frontend와 atlas가 같은 사람이라 배정하지만, SSoT에서 둘 중 하나의 owner가 바뀌어도 이미 열린 이슈는 `issues` 이벤트가 다시 오기 전까지 기존 담당자를 계속 표시한다. [triage_route.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_route.py:148), [atlas-triage.yml](/Users/jonghoPro/woo/00_project/08_BangCheck/.github/workflows/atlas-triage.yml:44) 설정 변경 시 전체 재실행을 강제/자동화하는 경로가 없다. 게다가 다음 실행에서 정정 코멘트는 `part`만 비교해 `미분류 → 미분류`라고 기록하고, 실제 변화인 “담당자 있음 → 미배정”, `basis` 변화는 숨긴다. [triage_comment.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_comment.py:197)

- **MAJOR — “2단으로 간다”는 운영상 아직 사실이 아니다.** 2단 코드가 `triage_route`를 import하고 결국 `triage_comment.py`를 실행하므로, *동일 checkout에서 실행된다면* 판정 함수를 공유하는 것은 맞다. [atlas_issue_analyzer.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/hermes/atlas_issue_analyzer.py:83), [atlas_issue_analyzer.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/hermes/atlas_issue_analyzer.py:245) 그러나 이 커밋의 워크플로에는 Hermes 호출·큐 등록·스케줄·배포 버전 검증이 없다. 경로 없는 9건은 1단에서 미분류 코멘트만 받고 자동으로 2단에 전달되지 않는다. Hermes의 별도 clone이 Actions checkout과 같은 routing version인지도 검증하지 않는다.

- **MAJOR — v1 은퇴는 멱등적인 “작업”이 아니라, 수동 재실행이 있어야만 수렴하는 부분 작업이다.** PATCH 하나가 실패하면 `gh()`가 즉시 `SystemExit(1)`을 내고 뒤의 24건은 시도하지 않는다. 다음 실행은 표시된 건을 건너뛰므로 재시도하면 회복할 수는 있지만, 실패·미처리 comment ID 목록/완료 개수의 요약·체크포인트가 없다. [retire_v1_comments.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/retire_v1_comments.py:64), [retire_v1_comments.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/retire_v1_comments.py:85) 더구나 이 스크립트를 호출하거나 검증하는 워크플로/테스트가 없다. “25건 은퇴”는 아직 실행 결과가 아니라 계획이다.

- **MAJOR — 두 comment 조회는 100건을 넘으면 JSON 파싱 자체가 깨진다.** `gh api --paginate`는 각 페이지를 별도 JSON 배열로 연속 출력하며, `--slurp` 없이는 `json.loads()` 가능한 단일 배열이 아니다. [triage_comment.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_comment.py:182), [retire_v1_comments.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/retire_v1_comments.py:56) 이슈별 댓글과 저장소 전체 v1 댓글이 각각 100건을 넘는 순간 실패한다. 특히 retire 대상은 저장소 전체 endpoint라 더 취약하다.

- **MINOR — `triageAuthors` 자체는 “PR만으로 누구나 봇 코멘트를 탈취”하는 권한 상승은 아니다.** workflow는 기본 브랜치의 파일을 실행하므로 외부 PR이 값을 바꿔도 merge 전에는 효력이 없고, default branch를 수정/merge할 수 있는 사람은 현재 `issues: write` workflow 자체도 바꿀 수 있다. 또한 임의 로컬 실행자는 `gh api user` 결과로 자기 계정만 추가하며, 원래 자기 댓글만 수정할 수 있다. [triage_comment.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/triage_comment.py:183) 다만 branch protection/CODEOWNERS로 이 YAML의 변경 주체가 workflow 변경 주체보다 넓다면 그때는 실제 신뢰 경계 확장이다. 최소한 human author 허용은 별도 보호 정책 또는 명시적 운영 credential에 묶어야 한다.

- **테스트 주장도 과장돼 있다.** 실행 결과 라우팅 테스트는 25건이 아니라 22건, 코멘트 테스트는 23건이었다. [test_triage_route.py](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/tools/test_triage_route.py:46) 라우팅 규칙 일부를 죽이면 실패하는 단위 검사는 있으나, 위의 목록 조회 실패, pagination, v1 부분 실패·재개, 설정 변경 후 재평가, 1→2단 전달은 전혀 붙들지 못한다.
```

## Concise summary

codex 실행 완료. Final response에서 verdict와 finding을 확인할 것.
