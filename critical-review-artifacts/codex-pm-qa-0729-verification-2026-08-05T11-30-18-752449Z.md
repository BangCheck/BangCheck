# codex critical-review artifact

- Provider: codex
- Model: gpt-5.6-terra
- Reasoning effort: high
- Target dir: /Users/jonghoPro/woo/00_project/08_BangCheck
- Exit code: 0
- Resolved CLI: /Users/jonghoPro/.local/bin/codex
- Started at (UTC): 2026-08-05T11:26:47.163427+00:00
- Finished at (UTC): 2026-08-05T11:30:18.749713+00:00

## Original task / label

pm-qa-0729-verification

## Final prompt

```text
[실행 조건] codex exec 비대화형이다. 되묻지 마라. 비용 승인 끝났다.
AGENTS.md STEP 1이 가리키는 _wood/workflows/_protocol.md 의 승인 게이트·preflight 는
이 요청에 적용되지 않는다. (Y/N)·확인 요청·메뉴 금지. 첫 출력부터 본문.

## 임무
PM이 2026-07-29 QA로 넘긴 5건이 **진짜 결함인지** 코드로 판정하라.
Claude가 1차 검증했다. 그 판정을 검증하고, 틀린 것을 잡고, 놓친 것을 찾아라.
마지막에 각 항목의 최종 판정과 registry 등록안(severity/disposition/evidence/relatedFeature)을 내라.

## PM 원문 (그대로)
```
(비로그인)
1. 체크리스트 선택 사항 저장 반영안됨
   체크리스트 항목 선택→저장→카드클릭(상세) : 선택 사항 일부가 저장이 안되어있음

(로그인)
2-1. step1에서 유형 선택 시 'step2 맞춤 체크리스트'에는 선택된 유형에 해당하는
     체크리스트만 제공 (현재는 기본 체크리스트도 노출되고 있음)
     ex) 벌레 회피형 선택 → (step2) '창문/망충망, 현관/문틈, 벌레 흔적,
         하수구/곰팡이 냄새' 만 노출
2-2. 'step3 추가로 확인할 항목'
   현재: 기본 항목, 내부 상태, 문제요소, 생활편의, 주변 환경
   변경 필요: 옵션(8개), 내부 상태(6개), 문제 요소(6개), 생활 편의(8개), 주변 환경(8개)
   ** 기본 항목이거나 위에서 선택한 유형에 해당하는 리스트의 ui는 선택된 항목으로 표시 필요
2-3. 나만의 항목 추가가 안됨
3. 방카드에 위치 아이콘 추가 필요 (클릭 시: 지도에서 해당 매물 마커 위치로 이동)
```

## Claude 1차 판정 (검증 대상)

**2-2 — 진짜 결함이라고 판정함.** 근거:
`frontend/src/features/customization/SettingsPage.tsx` STEP3 렌더가
`.filter((i) => !BASE_ITEM_LABELS.includes(i.label))` 로 기본 항목을 카테고리에서 뺀다.
그리고 `if (catConstItems.length === 0) return null` 로 빈 카테고리를 통째로 숨긴다.
실측 (constants.ts 파싱):
```
카테고리    전체  STEP3표시  PM요구
기본 옵션    8      0        8
내부 상태    6      6        6
문제 요소    6      5        6
안전/보안    8      0        —(PM 목록에 없음)
생활 편의    8      3        8
주변 환경    8      6        8
BASE_ITEM_LABELS = 24개
```
기본옵션·안전보안은 전 항목이 BASE라 카테고리가 사라진다.
PM이 안전/보안을 목록에 못 적은 것이 이것으로 설명된다.

**2-1 — 결함이 아니라 스펙 충돌이라고 판정함.** 근거:
`SettingsPage.tsx:78` 에 `// 스펙: STEP2 = BASE 15 + 유형 추천 (중복 제거, BASE 우선 정렬)`
주석이 있고 코드가 그대로 구현한다. PM 요구가 이 스펙과 충돌한다.
(부수 관측: 주석은 BASE 15인데 실제 BASE_ITEM_LABELS 는 24개다.)

**1 — 아직 미확정.** 확인한 것:
- `use-guest-room-store.ts` 의 `rawToRoomFields` 가 마지막에 `raw` 를 통째로 저장한다
- `ChecklistDetailPage.tsx:72-77` 이 `room.raw` 의 5개 섹션을 전부 복원한다
- `types/checklist.ts` 필드 합계 50개 (basic 13 / building 6 / interior 14 / safety 15 / custom 2)
- `computeRoomScore` 는 ratings 19 + problems 7 = 26 필드만 읽는다
저장 구조는 충분해 보이는데 PM은 "일부가 저장 안 됨"이라 한다. 어디서 잃는가?
zustand persist 직렬화, 폼 상태→raw 변환, 또는 상세 화면의 특정 섹션 렌더 누락을 의심한다.

**2-3 — 미확정.** 기존 registry 에 `BC-CHK-04`(나만의 항목 이름 미검증 → 409),
`BC-CHK-01`(없는 항목 삭제 시 500), `BC-CHK-05`, `BC-CHK-06` 이 있다.
GitHub 이슈 #207/#208 이 "커스텀 409" 이고 PR #209 가 2026-05-28 머지됐다.
7/29 QA 인데 다시 안 된다면 회귀이거나 다른 원인이다. 어느 쪽인가?

**3 — 결함이 아니라 기능 요청.** registry 의 defect 가 아니라 다른 자리에 가야 한다고 봄.

## 확인해야 할 것
1. Claude 의 2-2 판정이 맞는가. 계산과 코드 인용이 정확한가
2. Claude 의 2-1 판정(스펙 충돌)이 맞는가. 그 "스펙"의 출처가 저장소에 실재하는가
3. **1번의 실제 손실 지점을 특정하라.** 폼→store→상세 전 구간을 읽어라.
   `ChecklistNewPage.tsx`, `use-checklist-state.ts`, `use-guest-room-store.ts`,
   `ChecklistDetailPage.tsx`, `types/checklist.ts` 를 대조하라.
   손실이 없다면 "PM 관측이 재현되지 않는다"고 판정하라 — 지어내지 마라
4. **2-3 이 현행 코드에서 실제로 실패하는가.** `handleAddCustomItem`,
   `services/checklist-service.ts`, 백엔드 `ChecklistService` 의 custom item 경로,
   Flyway 마이그레이션의 unique 제약을 보라. 409 가 아직 나는 조건이 있는가
5. 3번을 registry 에 담을지. 담는다면 어떤 형태인가 (defect 가 아니면 무엇인가)
6. relatedFeature 귀속 — 각 항목이 어느 FT-*.yaml 에 붙어야 하는가.
   `.project-atlas/registry/FT-CHECKLIST-*.yaml`, `FT-ROOM-*.yaml`, `FT-MAP-*.yaml` 을 보라

## 출력
1) Claude 판정 중 틀린 것
2) 항목별 최종 판정 — 결함/스펙충돌/기능요청/재현불가 + 근거(파일:줄)
3) registry 등록안 — 각 항목의 id 후보·severity·disposition·evidence.path/symbol/line·relatedFeature
4) 놓친 결함
간결하게. 서술어체로 쓰되 근거 없는 문장은 쓰지 마라.

```

## Final response

```text
## Claude 판정 중 수정할 점

- **1번은 실제 결함이다.** 비로그인 저장 호출이 `answers`를 넘기지 않아 동적 체크값이 원본에 남지 않는다. 상세도 `answers`를 복원하지 않아 동적 항목은 모두 미선택으로 렌더된다.
- **2-2의 계산과 코드 위치는 맞다.** 다만 `0개`는 “STEP3 전체에서 미표시”가 아니라 “카테고리에서 제외되어 기본 항목 묶음으로 이동”이라는 뜻이다. 또한 7월 요구는 안전/보안 제외, 5월 기록은 안전/보안 포함이라 결함 확정 전 사양 결정이 필요하다.
- **2-1은 스펙 충돌 판정이 맞다.** 단, 근거는 코드 주석만이 아니다. 5월 QA 기록에 “기본+유형을 같이 제공” 요구가 실재한다. 다만 코드 주석의 `BASE 15`는 실제 `BASE_ITEM_LABELS` 24개와 불일치한다.
- **2-3은 미확정이 아니며 조건부 실제 실패가 있다.** 삭제한 커스텀 항목까지 한도 3개에 포함해, “3개 생성 → 1개 삭제 → 1개 추가”가 400으로 실패한다. 중복 이름 409는 현 Flyway에 `checklist_items.item_name` 유니크 제약이 없어 재현 근거가 없다.

## 항목별 최종 판정

| 항목 | 최종 판정 | 근거 |
|---|---|---|
| 1. 비로그인 선택 저장 누락 | **결함** | 저장 시 `answers`가 빠진다: [ChecklistNewPage.tsx:81](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/ChecklistNewPage.tsx:81). `GuestRoomRaw`에도 answers 필드가 없다: [room.ts:54](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/types/room.ts:54). 상세는 `answers`를 세팅하지 않고: [ChecklistDetailPage.tsx:73](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/ChecklistDetailPage.tsx:73), 렌더는 answers만 읽는다: [checklist-sections.tsx:413](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/components/checklist-sections.tsx:413). |
| 2-1. 유형 항목만 STEP2 노출 | **스펙 충돌** | 현재 구현은 공통 24개+유형 항목이다: [SettingsPage.tsx:78](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/SettingsPage.tsx:78), [constants.ts:113](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/constants.ts:113). 반대 요구(기본+유형)는 [5월 QA 기록:17](/Users/jonghoPro/woo/00_project/08_BangCheck/docs/qa/2026-5월-4째주/QA_로그인_이슈.md:17)에 실재한다. 7월의 “유형만”은 변경 요구다. |
| 2-2. STEP3 분류·개수 | **스펙 충돌** | 24개 base를 별도 “기본 항목”으로 표시하고, 카테고리에서는 제거한다: [SettingsPage.tsx:347](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/SettingsPage.tsx:347), [SettingsPage.tsx:399](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/SettingsPage.tsx:399). 결과는 옵션 `0+기본8`, 내부 `6`, 문제 `5+기본1`, 안전 `0+기본8`, 생활 `3+기본5`, 주변 `6+기본2`다. PM의 새 요구는 안전/보안을 제외하지만 기존 기록은 포함하므로 확정 사양이 먼저다. 선택 상태 자체는 유형 선택 시 base+유형을 active로 만드는 코드가 있다: [use-customization.ts:125](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/hooks/use-customization.ts:125). |
| 2-3. 나만의 항목 추가 | **결함, 조건부 재현** | 저장은 삭제 후 추가 순서다: [use-customization.ts:216](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/hooks/use-customization.ts:216). 화면은 삭제 항목을 빼지만: [use-customization.ts:114](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/customization/hooks/use-customization.ts:114), 서버 한도 계산은 `findByItemType(CUSTOM)`로 soft-delete 행까지 센다: [ChecklistService.java:148](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/domain/checklist/service/ChecklistService.java:148). 삭제 후 재추가가 3개 한도 400으로 막힌다. 409는 빈/null/100자 초과 등 DB 제약 위반 시 가능하지만, 중복 이름용 유니크 제약은 없다: [V6 migration:3](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/resources/db/migration/V6__create_checklist_tables.sql:3), [CustomItemCreateRequest.java:8](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/domain/checklist/dto/request/CustomItemCreateRequest.java:8). |
| 3. 방카드 위치 아이콘 | **기능 요청** | 홈 카드에는 위치 이동 콜백/아이콘이 없고: [RoomCard.tsx:8](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/components/RoomCard.tsx:8), 카드 클릭은 상세 이동뿐이다: [RoomsPage.tsx:490](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/rooms/pages/RoomsPage.tsx:490). 지도 내부 카드에는 이미 중심 이동이 있다: [MapPage.tsx:1047](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/map/MapPage.tsx:1047). 홈→지도 대상 전달 기능을 새로 설계해야 한다. |

## Registry 등록안

| 대상 | 등록안 |
|---|---|
| 1 | `BC-ROOM-06` (후보), **P1**, `FIX_PLANNED`, evidence: `ChecklistNewPage.tsx / handleSubmit / 81`; relatedFeature: `FT-ROOM-CREATE-WITH-CHECKLIST`. 상세 복원 누락도 같은 결함의 보조 근거로 `ChecklistDetailPage.tsx / guest load effect / 67`을 기록한다. |
| 2-1 | defect 등록 없음. 변경결정 이슈 `CR-CHK-STEP2-SCOPE`, relatedFeature: `FT-CHECKLIST-SETTINGS`. “유형만”과 “기본+유형” 중 하나를 확정해야 한다. |
| 2-2 | defect 등록 보류. 변경결정 이슈 `CR-CHK-STEP3-TAXONOMY`, relatedFeature: `FT-CHECKLIST-CATALOG`, evidence: `SettingsPage.tsx / STEP3 category rendering / 383`. 안전/보안 포함 여부와 base의 카테고리 귀속을 확정한 뒤 결함으로 전환한다. |
| 2-3 | `BC-CHK-07` (후보), **P2**, `FIX_PLANNED`, evidence: `ChecklistService.java / addCustomItem / 148`; relatedFeature: `FT-CHECKLIST-CUSTOM-ITEM`. 기존 `BC-CHK-04`는 별도로 유지한다(100자 초과·null 등 입력 검증 409). |
| 3 | defects registry 등록 없음. 기능 이슈 `FEAT-MAP-ROOM-FOCUS`. 실제 프런트 데이터 경로 기준 relatedFeature는 `FT-ROOM-LIST`; `FT-MAP-ROOM-OVERLAY`는 현재 프런트 소비 지점이 없는 서버 API라 이 UI 요구의 직접 귀속으로 부정확하다. |

## 놓친 결함

- 비로그인 1번은 단순한 부분 매핑 문제가 아니다. `answers` 전체가 저장 타입과 저장 호출에서 사라지고, 상세도 answers를 읽지 않는다. `deriveSafetyFromAnswers`는 항상 원본 safety를 그대로 반환하므로 안전·생활편의·주변환경 선택값은 보조 필드에도 남지 않는다: [mappers.ts:135](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/mappers.ts:135).
- 2-3의 soft-delete 한도 오산은 기존 `BC-CHK-01/04/05/06`에 없다. QA의 “추가 안 됨”을 정상 길이 입력에서도 설명할 수 있는 현행 결함이다.
```

## Concise summary

codex 실행 완료. Final response에서 verdict와 finding을 확인할 것.
