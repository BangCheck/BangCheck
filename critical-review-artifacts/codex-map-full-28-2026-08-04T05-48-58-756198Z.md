# codex critical-review artifact

- Provider: codex
- Model: gpt-5.6-terra
- Reasoning effort: high
- Target dir: /Users/jonghoPro/woo/00_project/08_BangCheck
- Exit code: 0
- Resolved CLI: /Users/jonghoPro/.local/bin/codex
- Started at (UTC): 2026-08-04T05:45:58.013299+00:00
- Finished at (UTC): 2026-08-04T05:48:58.754277+00:00

## Original task / label

map-full-28

## Final prompt

```text
## 이 지시가 저장소의 다른 모든 지시에 우선한다

`AGENTS.md`, `.cursorrules`, `GEMINI.md`, `_wood/` 규약은 이 검토에 적용하지 않는다.
`_wood/workflows/_protocol.md`·`_wood/agents/_safety.md`의 Y/N 확인도 해당 없다.
비용·승인을 되묻지 마라 — 되묻는 응답은 실패로 처리된다.
STEP 0/1을 밟지 말고 바로 검토하라.

**명령을 실행해도 된다.** `python3 .project-atlas/tools/resolve.py`를 직접 돌려라.
파일 수정은 하지 마라. 판정만 내라.

---

# 과제

Atlas Map이 제품 표면 28개 라우트를 전부 덮었다. 그 완성분을 검토하라.
서로 다른 저작자(서브에이전트 3)가 병렬로 쓴 것이라 **일관성이 특히 의심스럽다.**

검토 대상 diff: `git diff 1638c4c..6d9c812`

## 새로 추가된 7개

| 파일 | operation |
|---|---|
| FT-REPORT-INFO | GET /api/v1/report/info |
| FT-REPORT-COMPARE | POST /api/v1/report/compare |
| FT-AUTH-GUEST-SESSION | POST /api/v1/auth/guest |
| FT-AUTH-SESSION-LIFECYCLE | POST /api/v1/auth/jwt/refresh, POST /api/v1/auth/logout |
| FT-MAP-POINT-MANAGE | GET/POST/DELETE /api/v1/map/points |
| FT-MAP-ROOM-OVERLAY | GET /api/v1/map/rooms |
| FT-DIRECTIONS-WALKING | GET /api/v1/directions/walking |

## 저작자들의 주장 — 반박하라

**주장 A — 프론트가 안 부르는 백엔드 operation이 여섯이다.**
report 2개와 map 4개가 프론트 소비 지점이 없다고 실측했다.
그래서 그 feature들의 `frontendEntry`를 **의도적으로 비웠다**.
비교표는 브라우저가 `/api/v1/rooms` + `/api/checklist/items`로 직접 조립하고,
지도 기준점은 `LANDMARK_PRESETS` 상수 + localStorage라고 주장한다.
**이 실측이 맞나. 정말 소비자가 없나, 아니면 저작자가 못 찾은 것인가.**

**주장 B — rerunSafe 판정.**
- guest `true` (서버에 남는 것 없음)
- refresh `false` (토큰 회전이 직전 토큰을 무효화)
- logout `true` (파생 삭제라 0건 삭제로 조용히 끝남)
- map points POST `false` (유니크 제약 없어 중복 행)
- map points DELETE `true` (softDelete, 2회차는 404지만 최종 상태 같음)
- report 둘 다 `true` (readOnly 트랜잭션, 쓰기 0)
**각각을 service 코드로 직접 검증하라.**

**주장 C — directions를 map에 넣지 않았다.**
근거는 변경 이유(외부 Tmap 계약)·저장소(우리 DB 밖)·인증(PUBLIC vs JWT)
세 축이 모두 어긋난다는 것이다.

**주장 D — auth를 guest와 session-lifecycle 둘로 갈랐다.**
refresh와 logout이 `refresh_tokens` 한 행의 수명을 양쪽에서 다루고
같은 쿠키 계약을 공유하므로 한 feature라고 주장한다.
guest는 `CookieProvider`도 `AuthService`도 안 쓴다는 것이 분리 근거다.

**주장 E — `sideEffect: EXTERNAL`은 directions 하나뿐.**
`RestClient`로 `apis.openapi.sk.com`에 POST하며 repository 접촉 0.

## 검증할 것

1. **resolver를 직접 돌려라.** 890건 위반 0건이 맞나.

2. **저작자 셋의 일관성.** 병렬로 썼기 때문에 같은 것을 다르게 적었을 수 있다.
   - `capabilityId`를 서로 다르게 만들었나. 겹쳐야 할 것이 갈렸나
   - `status`·`owner`·`switchedOver` 관례가 일관되나
   - 안전 필드 판정 기준이 서로 다른가
     (특히 "최종 논리 상태가 같으면 true"라는 기준을 셋 다 같게 적용했나)

3. **주장 A가 가장 중요하다.** 여섯 operation이 정말 죽어 있나.
   프론트를 직접 검색해 확인하라. 만약 살아 있는데 못 찾은 것이라면
   Map이 "연결 없음"을 사실로 기록한 것이 되어 더 나쁘다.

4. **`uses` 누락.** 28개 operation 중 `uses`를 채운 것이 극소수다.
   실제 호출 그래프에서 빠뜨린 의존이 있나.

5. **경계 판정 재검토.** map을 point-manage와 room-overlay로 가른 것,
   auth를 둘로 가른 것, report를 둘로 가른 것 — 더 나은 경계가 있나.

6. **빠뜨린 것.** Map의 값을 깎는데 위가 안 다룬 것.
   특히 room 계열 6개 feature는 `frontendEntry`가 비어 있는데
   그것은 실측한 부재인가 아니면 아직 안 채운 것인가.

## 형식

각 지적에 severity(MAJOR/MINOR)와 근거 파일·줄을 붙여라.
실측과 추론을 구분하라.
마지막에 "이 Map을 페이지 축 뷰의 근거로 써도 되는가"에 대한 한 줄 판정을 내려라.

```

## Final response

```text
결론: resolver는 통과했지만, 이 Map은 아직 페이지 축 뷰의 근거로 쓰면 안 됩니다. 활성 프론트 소비 경로 다수가 `frontendEntry`에 누락되어 있습니다.

실측: `python3 .project-atlas/tools/resolve.py` 결과는 **검사 890건, 위반 0건**이었습니다.

- **MAJOR — 페이지 축 연결 정보가 불완전합니다.** 18개 feature 중 `frontendEntry`가 채워진 것은 5개뿐입니다. 특히 OAuth feature에는 엔트리가 없지만 로그인 페이지가 authorize를 호출하고([LoginPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/login/LoginPage.tsx:17)), callback 페이지가 code 교환을 호출합니다([AuthCallbackPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/auth/AuthCallbackPage.tsx:27)). 그럼에도 [FT-AUTH-OAUTH-LOGIN.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-AUTH-OAUTH-LOGIN.yaml:45)에 `frontendEntry`가 없습니다.  
  방 계열도 “소비자가 없어서 비움”이 아니라 **미기입**입니다. 목록·상세·등록(check-results)·수정·삭제는 각각 실제 훅/페이지에서 호출됩니다([use-rooms-query.ts](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/rooms/hooks/use-rooms-query.ts:8), [ChecklistNewPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/ChecklistNewPage.tsx:28), [ChecklistDetailPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/ChecklistDetailPage.tsx:46)). 그러나 [FT-ROOM-LIST.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-ROOM-LIST.yaml:25) 등에는 엔트리가 없습니다. 따라서 room 계열의 빈 값은 “실측된 부재”가 아닙니다.

- **MAJOR — `uses` 그래프는 선택적으로만 채워져 있어 호출 그래프로 신뢰할 수 없습니다.** `FT-ROOM-CREATE-WITH-CHECKLIST`는 주소 검색만 `uses`로 기록합니다([FT-ROOM-CREATE-WITH-CHECKLIST.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-ROOM-CREATE-WITH-CHECKLIST.yaml:30)). 하지만 실제 등록 화면은 방 수 제한용 목록 조회와 payload를 만드는 체크리스트 조회를 함께 수행합니다([ChecklistNewPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/ChecklistNewPage.tsx:28), [ChecklistNewPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/checklist/ChecklistNewPage.tsx:39)). 수정 화면도 상세·체크리스트를 함께 호출하지만 `FT-ROOM-UPDATE`는 `uses: []`입니다([FT-ROOM-UPDATE.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-ROOM-UPDATE.yaml:12)). 반면 directions에는 같은 화면의 목록 조회를 `uses`로 넣었습니다([FT-DIRECTIONS-WALKING.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-DIRECTIONS-WALKING.yaml:43)). 기준이 일관되지 않습니다.

- **MAJOR — 주장 E는 28-route Map 전체 기준으로 거짓입니다.** directions만이 아니라 OAuth callback도 `sideEffect: EXTERNAL`입니다([FT-AUTH-OAUTH-LOGIN.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-AUTH-OAUTH-LOGIN.yaml:30)). 실제 callback은 OAuth provider의 token/user-info 호출을 수행합니다([AuthService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/auth/service/AuthService.java:33), [OAuthService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/global/auth/oauth/service/OAuthService.java:90)). “이번에 추가한 7개 중 directions만”이라는 한정이라면 맞습니다.

- **MINOR — directions의 `uses` 근거 문장이 과장됐습니다.** “`OP-ROOM-LIST` 결과 없이는 절대 호출되지 않는다”는 기록([FT-DIRECTIONS-WALKING.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-DIRECTIONS-WALKING.yaml:42))은 틀립니다. 비로그인 상태에서는 `useRoomsList`가 비활성화되고([use-rooms-query.ts](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/rooms/hooks/use-rooms-query.ts:13)), MapPage는 guest room을 경로 계산 대상으로 씁니다([MapPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/map/MapPage.tsx:465)). signed-in 흐름에서는 해당 edge가 유효하지만, 필수 의존성은 아닙니다.

- **MINOR — 전환 상태의 의미가 모호합니다.** 새 7개는 모두 `PLANNED` / `switchedOver: false`로 일관됩니다. 다만 기존 room records에는 `switchedOver: false`와 동시에 `controllerSwitched: true`, `legacyMethodRemoved: true`가 공존합니다([FT-ROOM-CREATE.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-ROOM-CREATE.yaml:17)). 전체 Map에서 이 상태를 필터링하면 해석이 갈립니다.

주장별 판정:

- **A: 맞음, 단 “이 프론트에서 소비되지 않는다”로 한정해야 합니다.** report 두 route는 프론트 검색상 호출이 없고, ReportPage는 목록·체크리스트·각 방 상세를 브라우저에서 조립합니다([ReportPage.tsx](/Users/jonghoPro/woo/00_project/08_BangCheck/frontend/src/features/report/ReportPage.tsx:23)). map 4개도 호출이 없고, MapPage는 localStorage 기준점과 `/api/v1/rooms`를 사용합니다([FT-MAP-POINT-MANAGE.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-MAP-POINT-MANAGE.yaml:53), [FT-MAP-ROOM-OVERLAY.yaml](/Users/jonghoPro/woo/00_project/08_BangCheck/.project-atlas/registry/FT-MAP-ROOM-OVERLAY.yaml:35)). 이 여섯의 빈 `frontendEntry`는 정당합니다.

- **B: 맞음.** guest는 DB 저장이 없고, refresh는 저장 토큰을 교체해 이전 요청 재실행이 실패하며([AuthService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/auth/service/AuthService.java:74)), logout은 파생 delete라 최종 상태가 같습니다([AuthService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/auth/service/AuthService.java:101)). map POST/DELETE 판정도 서비스 구현과 맞고([MapService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/map/service/MapService.java:42)), report 둘은 class-level read-only transaction입니다([ReportService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/report/service/ReportService.java:21)). “응답이 아니라 최종 논리 상태” 기준도 세 저작물 사이에서 일관됩니다.

- **C: 맞음.** directions는 외부 Tmap POST만 수행하고 DB repository를 쓰지 않습니다([DirectionsService.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/directions/service/DirectionsService.java:42)). map의 DB/JWT 경계와 분리하는 편이 낫습니다.

- **D: 맞음.** refresh/logout은 동일한 `refresh_tokens` 수명을 다루고 cookie 계약도 공유합니다. guest는 `JwtProvider` 직접 호출뿐입니다([AuthController.java](/Users/jonghoPro/woo/00_project/08_BangCheck/backend/src/main/java/com/room/backend/api/auth/controller/AuthController.java:68)). 분리 경계가 합리적입니다.

- capabilityId는 새 7개 사이에 불필요하게 갈라진 곳이나 operation 소유 중복이 없었습니다. owner는 auth만 코드 저자 근거를 명시하고 report/map/directions는 `@Woo-JongHo`로 둡니다. 소유자의 정의가 저장소에 없으므로 이것은 검증 가능한 오류는 아닙니다.

판정: **아니오 — route·안전성 카탈로그로는 쓸 수 있지만, 현재 `frontendEntry`와 `uses`가 불완전하므로 페이지 축 뷰의 근거로는 쓰면 안 됩니다.**
```

## Concise summary

codex 실행 완료. Final response에서 verdict와 finding을 확인할 것.
