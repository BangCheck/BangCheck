// 본 컴포넌트의 default export는 사용되지 않음 (ChecklistNewPage는 DynamicChecklistSections 사용).
// 타입만 재export — 다른 모듈에서 import 가능하도록 유지.
import type { InteriorCheckData } from '@/types';
export type { InteriorCheckData };
