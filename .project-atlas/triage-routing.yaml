# 이슈 → 파트·담당자 라우팅 규칙
#
# 왜 저장소에 두는가
#   담당이 바뀔 때마다 봇을 다시 배포하지 않기 위해서다. 봇은 이 파일을 읽고
#   판정한다. 규칙을 봇 코드에 박으면 사람이 바뀔 때마다 코드 변경·배포가 따라온다.
#
# 왜 FT 의 owner 를 쓰지 않는가
#   FT-*.yaml 의 owner 는 그 기능을 만든 사람이다. 결함을 고칠 사람과 다를 수 있고,
#   registry 는 "결함 수정 담당"을 선언한 적이 없다. 두 개념을 한 필드로 쓰면
#   나중에 갈라진다.
#
# 판정 순서
#   rules 를 위에서부터 훑어 **처음 맞는 규칙**을 쓴다. 좁은 규칙을 위에 둔다.
#   checklist 규칙이 frontend 규칙보다 위에 있어야 하는 이유가 그것이다.
#
# 봇이 이 파일로 하는 일과 하지 않는 일
#   한다   — 코멘트로 파트·담당자를 **제안**한다
#   안 한다 — GitHub assignee 를 실제로 바꾸지 않는다. 라벨도 달지 않는다.
#            registry 도 고치지 않는다. 제안은 사람이 보고 반영한다.

version: 1

# 이 값이 바뀌면 봇 코멘트의 판정도 바뀐 것으로 본다.
# 코멘트 토큰에 routingVersion 으로 기록돼, 규칙이 개정된 뒤 다시 판정됐는지
# 사람이 구별할 수 있다.
routingVersion: "2026-08-06.1"

# 담당이 나뉘는 축이 프론트와 백엔드에서 다르다.
#   프론트 — 한 사람이 전부 맡는다. 경로를 더 쪼갤 이유가 없다
#   백엔드 — 도메인으로 나뉜다. 로그인 / 체크리스트 / 나머지
parts:
  frontend: 프론트엔드
  backend-auth: 백엔드 · 로그인·인증
  backend-checklist: 백엔드 · 체크리스트
  backend: 백엔드 · 그 외
  atlas: Atlas·하네스

# 근거가 될 수 없는 경로.
#
# registry 는 결함이 **기록된** 곳이지 **나는** 곳이 아니다. 이슈가 자기 Atlas
# 기록을 인용하는 것은 정상인데, 그것을 근거로 세면 인용한 이슈가 전부 Atlas
# 파트와 충돌한다.
# 2026-08-06 실측: 충돌 9건 중 8건이 `defects.yaml` 언급 하나 때문이었다.
# 규칙보다 먼저 걸러야 한다 — 규칙 안에서 처리하면 "첫 매칭이 이긴다"에 걸려
# registry 가 파트를 차지한다.
ignore:
  - ".project-atlas/registry/**"
  - ".project-atlas/schema.yaml"
  - ".project-atlas/project.yaml"

# 첫 매칭이 이긴다. 좁은 것부터.
rules:
  # ── Atlas·하네스 — 제품이 아니라 도구다 ──
  # 오늘 만든 이슈 상당수(#217·#220·#221·#224)가 여기 속하는데, 제품 결함과
  # 섞이면 registry 가 무엇의 지도인지 흐려진다. 파트로 갈라 둔다.
  # frontend/** 보다 위에 있어야 Atlas UI 경로가 프론트로 흡수되지 않는다.
  - part: atlas
    assignee: "@Woo-JongHo"
    paths:
      - ".project-atlas/**"
      - ".github/**"
      - ".claude/**"
      - "_wood/**"
      - "frontend/src/features/project-atlas/**"
      - "frontend/src/features/research/**"
      # 스냅샷 검사 스크립트는 제품이 아니라 하네스다.
      - "frontend/scripts/*atlas*"

  # ── 백엔드 · 로그인·인증 ──
  - part: backend-auth
    assignee: "@dlwldP"
    paths:
      - "backend/src/main/java/com/room/backend/api/auth/**"
      - "backend/src/main/java/com/room/backend/global/auth/**"
      - "backend/src/main/java/com/room/backend/domain/user/**"

  # ── 백엔드 · 체크리스트 ──
  - part: backend-checklist
    assignee: "@std-yong"
    paths:
      - "backend/src/main/java/com/room/backend/domain/checklist/**"
      - "backend/src/main/java/com/room/backend/api/**/checklist**"

  # ── 백엔드 · 그 외 (지도·방 목록·리포트·주소 등) ──
  - part: backend
    assignee: "@minwoo-l"
    paths:
      - "backend/**"

  # ── 프론트 전체 ──
  # 프론트는 도메인을 나누지 않는다. 한 사람이 전부 맡는다.
  - part: frontend
    assignee: "@Woo-JongHo"
    paths:
      - "frontend/**"

# 어느 규칙에도 안 걸리거나 근거가 없을 때.
# 임의로 하나를 고르지 않는다 — 틀린 배정은 잘못된 사람을 부르고,
# 그 코멘트는 봇이 낼 수 없는 권위를 주장하게 된다.
fallback:
  part: null
  assignee: null
  note: "소스 경로와 명시적 영역 정보가 없어 근거 있게 특정할 수 없습니다."

# 경로가 여러 규칙에 걸릴 때.
# 예: 이슈가 frontend/features/checklist/ 와 backend/api/room/ 을 함께 언급
# 하나를 임의로 고르지 않고 사람에게 넘긴다.
conflict:
  part: null
  assignee: null
  note: "여러 파트에 걸쳐 있어 하나로 특정하지 않습니다. 담당을 사람이 정해 주세요."

# 이 계정들이 쓴 코멘트만 봇 코멘트로 인정하고 갱신한다.
#
# 왜 봇 계정만으로는 부족한가
#   1단은 Actions(`github-actions[bot]`)에서 돌지만 2단은 Hermes 머신에서
#   사람 계정의 gh 토큰으로 돈다. 계정이 갈리는데 1단이 봇 계정만 인정하면
#   2단이 단 코멘트를 못 찾아 같은 이슈에 두 번째 코멘트를 만든다.
#
# 왜 토큰만 보고 갱신하지 않는가
#   토큰은 코멘트 본문이라 누구나 복사해 쓸 수 있다. 아무 코멘트나 갱신하면
#   봇이 남의 글을 덮어쓴다. 작성자를 함께 본다.
triageAuthors:
  - "github-actions[bot]"
  - "github-actions"
  - "Woo-JongHo"

# ── 관계 탐지 (triage_relate.py) ──────────────────────────────
#
# 봇이 세는 신호는 넷뿐이고 전부 검증 가능한 사실이다.
#   ① 이슈 본문·코멘트의 `#N`   ② 타임라인 역참조
#   ③ 열린 PR 의 `closes #N`     ④ 이슈 경로 ∩ 열린 PR 변경 파일
# 제목 유사도 같은 추정 신호는 넣지 않는다 — v1 이 그것으로 중복을 추정하다
# 은퇴했다. 여기 값을 늘려서 추정 신호를 켤 수 있는 구멍도 두지 않았다.
#
# 이 블록을 고쳐도 routingVersion 은 올리지 않는다.
#   routingVersion 은 **파트·담당자 판정**의 판본이다. 관계 탐지는 그 판정에
#   손대지 않으므로, 여기를 고쳤다고 버전을 올리면 열린 이슈 전부에
#   "분류 정정" 코멘트가 쏟아진다. 바뀐 것은 배정이 아니다.
relate:
  # 변경 파일을 대조할 열린 PR 상한. 넘으면 최근 갱신순으로 자르고
  # **잘랐다는 사실을 코멘트에 적는다** — 조용히 자르면 "겹침 없음"이
  # "안 봤음"과 구별되지 않는다.
  maxOpenPullRequests: 40

  # 코멘트에 적을 관계 상한. 열 몇 건을 나열하면 사람이 안 읽는다.
  maxRelations: 8

  # 한 PR 당 코멘트에 적을 겹친 경로 수. 나머지는 "외 N건"으로 접는다.
  maxOverlapPathsShown: 3

  # 교집합 근거로 세지 않는 공용 파일.
  #
  # `backend/build.gradle` 은 수십 PR 과 겹친다. 그런 파일이 걸리면 모든 이슈가
  # 모든 PR 과 연관돼 보이고, 그 순간 이 섹션은 신호가 아니라 배경 소음이 된다.
  # 담당이 바뀌면 rules 만 고치듯, 노이즈가 바뀌면 여기만 고친다.
  sharedPaths:
    - "backend/build.gradle"
    - "backend/settings.gradle"
    - "backend/gradle.properties"
    - "backend/gradlew*"
    - "frontend/package.json"
    - "frontend/package-lock.json"
    - "frontend/pnpm-lock.yaml"
    - "frontend/vite.config.*"
    - "frontend/tsconfig*.json"
    - "frontend/eslint.config.*"
    - "**/README.md"
    - ".project-atlas/registry/**"

  # 선언으로 다 못 잡는 몫. 열린 PR 중 이 수 이상이 건드린 경로는
  # 그 시기의 공용 파일로 보고 근거에서 뺀다.
  # 0 이면 끈다. 열린 PR 이 적을 때 너무 낮게 잡으면 진짜 겹침이 지워진다 —
  # 지금 저장소의 열린 PR 이 한 자릿수라 5 로 둔다.
  commonPathPrThreshold: 5

  # PR 변경 파일 캐시 수명(초).
  #
  # 왜 캐시가 필요한가: 소급 적용은 이슈마다 triage_comment.py 를 **새 프로세스**로
  # 띄운다(atlas-triage.yml 의 `for n in $numbers`). 캐시가 프로세스 안에 있으면
  # 그 루프에서 한 번도 재사용되지 않아 열린 이슈 200건 × 열린 PR 40건 =
  # 8,000 회 호출이 그대로 난다. 그래서 캐시는 파일이고, 키는 번호+head SHA 다.
  cacheTtlSeconds: 900

# ── 근거 ──────────────────────────────────────────────────────
#
# 2026-08-05 사용자 확정:
#   "frontend - woojongho, backend 중에 로그인 → dlwldP /
#    체크리스트 관련 → std-yong / 나머지 백엔드 지도라던가 방목록이라던가 → minwoo-l"
#
# GitHub 로그인은 이슈 작성 이력으로 확인했다 —
# Woo-JongHo · dlwldP · std-yong · minwoo-l 네 계정이 실재한다.
#
# 담당이 바뀌면 이 파일만 고친다. 봇 재배포는 필요 없다.
